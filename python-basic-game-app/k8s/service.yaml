apiVersion: v1
kind: Service
metadata:
  name: python-game-service
  namespace: gameapp
spec:
  selector:
    app: python-game-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
